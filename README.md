# mind-store
mind-store is a lightweight Git-backed system for organizing ideas and todos. Each item is stored as a simple file with metadata for tags, priority, and status. The CLI lets you add, edit, search, and sync entries.
